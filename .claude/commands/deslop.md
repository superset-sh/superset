---
description: Shared command wrapper for deslop.md
---

Use the shared command spec at `./.agents/commands/deslop.md` as the single source of truth.

Execute that spec exactly. Pass through any provided `$ARGUMENTS` unchanged.
