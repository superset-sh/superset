/**
 * The module specifier widget authors import the kit from, e.g.
 *   import { tokens } from "superset/widgets";
 * The renderer's require shim resolves ONLY this name plus "react" and
 * "react-icons/lu"; anything else throws.
 */
export const WIDGET_KIT_MODULE_NAME = "superset/widgets";
