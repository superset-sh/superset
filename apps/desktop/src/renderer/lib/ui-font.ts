// Set on <html> by UiFontSync; consumed by the `body` rule in globals.css,
// which falls back to the theme stack when the setting is unset.
export const UI_FONT_FAMILY_CSS_VAR = "--superset-ui-font-family";

// Shown as the placeholder in Settings when no override is set. Matches the
// Tailwind `--font-sans` default the body inherits.
export const DEFAULT_UI_FONT_FAMILY =
	'ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"';
