import { type ComponentProps } from "react";
import { QRCodeSVG } from "qrcode.react";

/* -------------------------------------------------------------------------------------------------
 * Types
 * -----------------------------------------------------------------------------------------------*/

interface Props extends ComponentProps<typeof QRCodeSVG> {}

/* -------------------------------------------------------------------------------------------------
 * QRCode
 * -----------------------------------------------------------------------------------------------*/

export function QRCode({ size = 128, ...props }: Props) {
  return <QRCodeSVG size={size} {...props} />;
}
